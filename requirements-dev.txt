# Development-only dependencies (Python test suite + linting).
# Runtime dependencies for the extension backends are documented in
# docs/installation.md and mirrored in applications/docker/environment.yml.
pytest>=8
pytest-cov>=5
numpy
ruff==0.15.19
